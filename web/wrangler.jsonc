{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "kindling-web",
  "main": "./dist/_worker.js/index.js",
  "compatibility_date": "2026-08-15",
  "compatibility_flags": ["nodejs_compat"],
  "assets": {
    "binding": "ASSETS",
    "directory": "./dist",
    "not_found_handling": "404-page",
    // Browser navigations prefer the static 404 over the Worker. Share pages
    // are SSR, so they must run first or /s/:id never loads.
    "run_worker_first": ["/s/*"]
  },
  "services": [
    {
      "binding": "API",
      "service": "kindling-api"
    }
  ],
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1
  },
  "kv_namespaces": [
    { "binding": "SESSION" }
  ]
}
